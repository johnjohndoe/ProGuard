<!-- This Ant build file illustrates how to process applets.
     Usage: ant -f examples/ant/applets.pro -->

<project name="Applets" default="obfuscate" basedir="../..">

<target name="obfuscate">
   <taskdef name="proguard" classname="proguard.ant.ProGuardTask"
            classpath="/usr/local/java/proguard/lib/proguard.jar" />

   <proguard printseeds="on"
             outjar="out.jar">
      <libraryjar name="${java.home}/lib/rt.jar" />
      <!-- Specify your library jars here. -->
      <!-- Specify your input jars here. -->
      <injar name="in.jar" />

      <!-- Keep all public applets. -->
      <keep access="public" extends="java.applet.Applet" />

      <!-- Your application may contain more items that need to be preserved;
        typically classes that are dynamically created using Class.forName -->
   </proguard>
</target>

</project>
